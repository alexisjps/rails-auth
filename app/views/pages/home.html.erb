<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<% if user_signed_in? %>
  <%= current_user.username %>
<% else %>
  <p>Connecte toi sinon tu pars ou Xavier vient chez toi</p>
  <%= link_to 'Log in', new_user_session_path, class: 'btn btn-primary' %>
<% end %>